# =============================================================================
# HASANAT — Environment Variables
# Copy to .env and fill in your values. NEVER commit .env to git.
# =============================================================================

# --- Supabase (exposed to client via EXPO_PUBLIC_ prefix) ---
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# --- AWS S3 (Edge Function secrets — NOT exposed to client) ---
# Set these via Supabase CLI:
#   npx supabase secrets set AWS_REGION=us-east-1 AWS_BUCKET_NAME=hasanat-uploads-ali-2026 AWS_ACCESS_KEY_ID=YOUR_KEY AWS_SECRET_ACCESS_KEY=YOUR_SECRET
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_BUCKET_NAME=hasanat-uploads

# --- AlAdhan API (public, no key needed) ---
# Base URL is hardcoded in src/lib/aladhan.ts
# https://api.aladhan.com/v1
